// I realised the code still works without this file. Feel free to delete or maintain this one. 
// your choice, really...


/*chrome.runtime,onMessage.addListener((request, sender, sendResponse) => {
    if (request.action === "showAlert") {
        alert("Warning: You are visiting a phishing site!");
    }
});*/